
---
import Layout from './Custom';

const { frontmatter } = Astro.props;
---
<Layout title={frontmatter.title}><slot /></Layout>



<!-- ---
import type { MDXLayoutProps } from 'astro';

type Props = MDXLayoutProps<{
    title: string;
	body: string;
}>;

const {	frontmatter } = Astro.props;
---

<html lang="en">
    <head></head>
    <body>
        <h1>{frontmatter.title}</h1>
        <slot />
    </body>
</html> -->